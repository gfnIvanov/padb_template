name: Main

on: [push]

jobs:
  flake-install:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v3
        - uses: actions/setup-python@v4
          with:
            python-version: "3.9"
        - run: pip install flake8
        - run: echo "Flake8 is installed!"
